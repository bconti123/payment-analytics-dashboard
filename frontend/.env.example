# Base URL of the FastAPI backend (must include the /api/v1 prefix).
# Vars prefixed with NEXT_PUBLIC_ are inlined into the client bundle at build time.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
